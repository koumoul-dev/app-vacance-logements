{
  "type": "object",
  "allOf": [
    {
      "title": "Source de données",
      "required": [ "datasets" ],
      "properties": {
        "datasets": {
          "$ref": "#/definitions/datasets"
        }
      }
    }
  ],
  "definitions": {
    "datasets": {
      "type": "array",
      "items": [
        {
          "title": "Données INSEE",
          "description": "Jeu de données utilisé pour la première et deuxième vignette",
          "type": "object",
          "x-fromUrl": "api/v1/datasets?status=finalized&field-type=integer,number&q={q}&select=id,title&owner={context.owner.type}:{context.owner.id}",
          "x-itemsProp": "results",
          "x-itemTitle": "title",
          "x-itemKey": "href",
          "properties": {
            "href": {
              "type": "string"
            },
            "title": {
              "type": "string"
            },
            "id": {
              "type": "string"
            }
          }
        },
        {
          "title": "Données Lovac",
          "description": "Jeu de données utilisé pour la troisième vignette",
          "type": "object",
          "x-fromUrl": "api/v1/datasets?status=finalized&field-type=integer,number&q={q}&select=id,title&owner={context.owner.type}:{context.owner.id}",
          "x-itemsProp": "results",
          "x-itemTitle": "title",
          "x-itemKey": "href",
          "properties": {
            "href": {
              "type": "string"
            },
            "title": {
              "type": "string"
            },
            "id": {
              "type": "string"
            }
          }
        }
      ]
    }
  }
}
